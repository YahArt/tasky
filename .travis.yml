language: node_js
node_js:
    - "10"
cache:
    directories:
        - "node_modules"
notifications:
  email:
    recipients:
      - hutteryannick@gmail.com
    on_success: always
    on_failure: always

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
